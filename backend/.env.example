# Ollama (primary LLM — free, local)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# Gemini (fallback only — used when Ollama is unavailable)
GEMINI_API_KEY=

# Server
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000